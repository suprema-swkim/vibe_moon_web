---
description: Flutter 프로젝트의 폴더 구조를 정의한 가이드라인
globs: **/*.dart
# alwaysApply: true
---

# 📁 프로젝트 구조 규칙 (Project Structure Rules)

이 파일은 Flutter 프로젝트의 일관된 폴더 구조와 파일 명명 규칙을 정의합니다. AI와 개발자는 새로운 파일이나 기능을 추가할 때 이 구조를 따라야 합니다.

## 1. 📂 전체 프로젝트 구조

```
lib/
├── main.dart                    # 앱 진입점
├── app/                         # 앱 레벨 설정 및 라우팅
│   ├── app.dart                 # App 위젯 정의
│   ├── router.dart              # go_router 라우팅 설정
│   └── theme/                   # 앱 테마 설정
│       ├── app_theme.dart
│       └── theme_extensions.dart
├── core/                        # 공통 유틸리티 및 상수
│   ├── constants/               # 상수 정의
│   │   ├── api_constants.dart
│   │   ├── app_constants.dart
│   │   └── asset_constants.dart
│   ├── utils/                   # 유틸리티 함수
│   │   ├── logger.dart
│   │   ├── date_formatter.dart
│   │   └── validators.dart
│   ├── extensions/              # Dart 확장 메서드
│   │   ├── string_extension.dart
│   │   ├── datetime_extension.dart
│   │   └── context_extension.dart
│   └── errors/                  # 에러 처리
│       ├── exceptions.dart
│       └── failures.dart
├── data/                        # 데이터 계층
│   ├── models/                  # 데이터 모델 (JSON 직렬화)
│   │   ├── branch_model.dart
│   │   ├── branch_model.g.dart
│   │   └── ...
│   ├── repositories/            # Repository 구현체
│   │   ├── branch_repository.dart
│   │   └── ...
│   └── sources/                 # 데이터 소스 (API, Local DB)
│       ├── remote/              # 원격 데이터 소스
│       │   ├── api_client.dart
│       │   └── branch_api.dart
│       └── local/               # 로컬 데이터 소스
│           └── local_storage.dart
├── domain/                      # 도메인 계층 (비즈니스 로직)
│   ├── entities/                # 도메인 엔티티
│   │   ├── branch.dart
│   │   └── ...
│   ├── repositories/            # Repository 인터페이스
│   │   ├── i_branch_repository.dart
│   │   └── ...
│   └── usecases/                # 유스케이스 (선택적)
│       └── get_branch_list.dart
└── presentation/                # 프레젠테이션 계층
    ├── common/                  # 공통 UI 컴포넌트
    │   ├── widgets/             # 재사용 가능한 위젯
    │   │   ├── custom_button.dart
    │   │   ├── loading_indicator.dart
    │   │   └── error_view.dart
    │   └── layouts/             # 레이아웃 위젯
    │       └── main_layout.dart
    └── features/                # 기능별 UI 구성
        ├── branch/              # Branch 기능
        │   ├── pages/           # 페이지 위젯
        │   │   ├── branch_list_page.dart
        │   │   └── branch_detail_page.dart
        │   ├── widgets/         # 기능 전용 위젯
        │   │   ├── branch_card.dart
        │   │   └── branch_filter.dart
        │   └── providers/       # Riverpod 프로바이더
        │       ├── branch_list_provider.dart
        │       └── branch_detail_provider.dart
        ├── place_group/         # PlaceGroup 기능
        │   ├── pages/
        │   ├── widgets/
        │   └── providers/
        └── place/               # Place 기능
            ├── pages/
            ├── widgets/
            └── providers/
```

## 2. 📝 파일 및 폴더 명명 규칙

### 파일 명명 규칙

* **snake_case** 사용 (소문자 + 언더스코어)
  - 예: \`branch_list_page.dart\`, \`user_profile_model.dart\`, \`api_client.dart\`

* **파일 이름은 내용을 명확히 표현**
  - 좋은 예: \`branch_detail_page.dart\`
  - 나쁜 예: \`page1.dart\`, \`temp.dart\`

* **접미사 규칙 (Suffix Conventions)**
  - 페이지: \`*_page.dart\` (예: \`branch_list_page.dart\`)
  - 위젯: \`*_widget.dart\` 또는 명확한 이름 (예: \`branch_card.dart\`, \`custom_button.dart\`)
  - 모델: \`*_model.dart\` (예: \`branch_model.dart\`)
  - Provider: \`*_provider.dart\` (예: \`branch_list_provider.dart\`)
  - Repository 구현체: \`*_repository.dart\` (예: \`branch_repository.dart\`)
  - Repository 인터페이스: \`i_*_repository.dart\` (예: \`i_branch_repository.dart\`)
  - API 클라이언트: \`*_api.dart\` (예: \`branch_api.dart\`)
  - 유틸리티: \`*_util.dart\` 또는 명확한 이름 (예: \`logger.dart\`, \`validators.dart\`)
  - 확장 메서드: \`*_extension.dart\` (예: \`string_extension.dart\`)
  - 상수: \`*_constants.dart\` (예: \`api_constants.dart\`)

* **생성된 파일**
  - JSON 직렬화: \`*.g.dart\` (예: \`branch_model.g.dart\`)
  - 생성된 파일은 git에 커밋하지 않음 (\`.gitignore\`에 추가)

### 폴더 명명 규칙

* **snake_case** 사용 (소문자 + 언더스코어)
  - 예: \`place_group/\`, \`user_profile/\`, \`common_widgets/\`

* **기능별로 명확히 분리**
  - 각 기능(feature)은 독립적인 폴더로 구성
  - 예: \`features/branch/\`, \`features/place_group/\`

* **복수형 사용 규칙**
  - 여러 파일을 담는 컨테이너 폴더: 복수형 사용
    - 예: \`pages/\`, \`widgets/\`, \`models/\`, \`providers/\`
  - 기능 단위 폴더: 단수형 사용
    - 예: \`branch/\`, \`place/\`, \`user/\`

* **계층 구조 준수**
  - \`presentation/features/{feature_name}/\` 구조 유지
  - 각 feature 내부는 \`pages/\`, \`widgets/\`, \`providers/\` 하위 폴더로 구성

### 클래스 및 변수 명명 규칙

* **클래스명: PascalCase**
  - 예: \`BranchListPage\`, \`BranchModel\`, \`BranchRepository\`

* **변수 및 함수명: camelCase**
  - 예: \`branchId\`, \`placeGroupName\`, \`fetchBranchList()\`

* **상수: lowerCamelCase** (Dart 권장)
  - 예: \`const apiBaseUrl = 'https://api.example.com';\`
  - 예: \`const maxRetryCount = 3;\`

* **Private 멤버: 언더스코어 접두사**
  - 예: \`_privateMethod()\`, \`_internalState\`

### 도메인 용어 사용 규칙

* **development-dictionary.mdc에 정의된 용어 사용**
  - \`Branch\`, \`PlaceGroup\`, \`Place\` 용어를 일관되게 사용
  - 축약하지 않고 전체 이름 사용
    - 좋은 예: \`branchId\`, \`placeGroupId\`, \`placeGroupName\`
    - 나쁜 예: \`brId\`, \`pgId\`, \`pgName\`
  
* **계층 구조 반영**
  - Branch → PlaceGroup → Place 관계 명확히 표현
  - 예: \`BranchPlaceGroupList\`, \`PlaceGroupPlaceList\`
  
* **일관된 접미사 사용**
  - 목록: \`*List\` (예: \`branchList\`, \`placeGroupList\`)
  - 상세: \`*Detail\` (예: \`branchDetail\`, \`placeDetail\`)
  - 개수: \`*Count\` (예: \`placeGroupCount\`, \`placeCount\`)
