---
alwaysApply: true
---

# 🎨 UI/UX 가이드라인 - 글래스모피즘 2.0 (Glassmorphism 2.0)

이 파일은 Flutter Web 프로젝트에서 글래스모피즘 2.0 디자인 트렌드를 기반으로 한 일관되고 현대적인 사용자 경험을 제공하기 위한 UI/UX 규칙을 정의합니다.

## 1. 🎯 UI/UX 철학

### 핵심 원칙
1. **투명성과 깊이감** - 레이어드 투명도로 공간감과 계층 구조 표현
2. **부드러운 인터랙션** - 블러와 그라데이션을 활용한 자연스러운 전환
3. **반응형 웹 우선** - 데스크톱, 태블릿, 모바일 모든 환경 최적화
4. **시각적 일관성** - 글래스 효과의 통일된 적용
5. **성능과 미학의 균형** - 웹 성능을 고려한 최적화된 블러 효과

### 글래스모피즘 2.0의 특징
- **배경 블러(Backdrop Blur)**: 뒤 배경이 흐릿하게 보이는 효과
- **반투명 레이어**: 투명도를 활용한 깊이감 (Cloudy Glass)
- **섬세한 테두리**: 미묘한 하이라이트 테두리
- **파스텔 그라데이션**: 부드러운 핑크, 피치 계열의 배경
- **둥근 모서리**: 넉넉한 Border Radius (20px+)

## 2. 🎨 디자인 시스템

### 색상 팔레트 (Glassmorphism 2.0)

```dart
// lib/app/theme/app_colors.dart
import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors {
  // === Pastel Colors (New Design Concept) ===
  static const Color pastelPink = Color(0xFFFFD1DC);
  static const Color pastelPurple = Color(0xFFE0BBE4);
  static const Color pastelPeach = Color(0xFFFFDFD3);
  static const Color pastelBlue = Color(0xFFB5EAEA);
  static const Color deepPurple = Color(0xFF2D2B55); // Primary Button Color

  // === Glass Surface Colors (카드, 패널 등) ===
  static const Color glassSurface = Color(0x1AFFFFFF); // 10% white
  static const Color glassSurfaceLight = Color(0x80FFFFFF); // 50% white (Light Mode)
  static const Color glassSurfaceDark = Color(0x26FFFFFF); // 15% white
  
  // === Text Colors ===
  static const Color textPrimaryLight = Color(0xFF2D3436); // Dark Grey
  static const Color textSecondaryLight = Color(0xB32D3436);
  
  // === Gradient Presets ===
  static const LinearGradient pastelBgGradient = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [
      Color(0xFFF8Cdda), // Soft Pink
      Color(0xFFFdd6bd), // Peach
      Color(0xFFC9D6FF), // Soft Blue/Purple
    ],
    stops: [0.0, 0.5, 1.0],
  );
}
```

### 블러 및 효과 상수

```dart
// lib/app/theme/app_effects.dart
import 'dart:ui';
import 'package:flutter/material.dart';

class AppEffects {
  // === Blur Values ===
  static const double blurLight = 10.0;
  static const double blurMedium = 20.0;
  static const double blurHeavy = 30.0;
  
  // === Border Radius ===
  static const double radiusMedium = 20.0; // Increased for softer look
  static const double radiusLarge = 32.0;
  
  // === Glass Effect Presets ===
  static BoxDecoration glassCard({
    Color? color,
    double blur = blurMedium,
    double borderRadius = radiusMedium,
  }) {
    return BoxDecoration(
      color: color ?? AppColors.glassSurfaceLight, // Lighter surface by default
      borderRadius: BorderRadius.circular(borderRadius),
      border: Border.all(
        color: AppColors.borderGlassLight,
        width: 1.0,
      ),
      boxShadow: [
        BoxShadow(
          color: AppColors.shadowLight, // Softer shadow
          blurRadius: 15,
          offset: const Offset(0, 8),
        ),
      ],
    );
  }
}
```

### 타이포그래피 (웹 최적화)

```dart
// lib/app/theme/app_typography.dart
class AppTypography {
  // Clean Sans-Serif Fonts
  static const TextStyle h1 = TextStyle(
    fontSize: 32,
    fontWeight: FontWeight.bold,
    color: AppColors.textPrimaryLight,
  );
  
  static const TextStyle bodyMedium = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.normal,
    color: AppColors.textPrimaryLight,
  );
}
```

## 3. 📱 반응형 웹 디자인

(기존 내용 유지)

## 4. 🧩 글래스모피즘 UI 컴포넌트

(기존 내용 유지, 단 스타일은 위 색상과 효과를 따름)

## 10. 💡 글래스모피즘 베스트 프랙티스

### 디자인 원칙
1. **배경 중요성**: 파스텔 톤의 그라데이션 배경이 핵심입니다. 너무 강렬한 색상보다는 부드러운 전환을 사용하세요.
2. **투명도 균형**: 라이트 모드에서는 50~80%의 흰색 불투명도를 사용하여 가독성을 높이고 깔끔한 느낌을 줍니다.
3. **그림자**: 진한 그림자 대신 넓고 연한 그림자(Diffuse Shadow)를 사용하여 공중에 떠 있는 느낌을 줍니다.

---

**핵심 원칙**: "부드럽고, 화사하며, 현대적인 글래스모피즘"
